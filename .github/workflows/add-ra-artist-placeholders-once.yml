name: Archived placeholder artist report

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The placeholder artist report has already been generated."
